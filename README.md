# hola talk


## setup

this repo uses atlas. if you have a recent nim installation (grab it with grabnim) you should be able to make it work with `atlas install`.
but it is the first time I am using atlas (and grabnim) so no idea if it actually works.
